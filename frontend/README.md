# Frontend

React + Vite frontend for the transfer-function analyzer.

## Run

```shell
npm install
npm run dev
```

The main page lets users enter numerator and denominator coefficients from `s^4` to `s^0`, detect parameters, configure one scan parameter, and animate the returned step-response frames.
